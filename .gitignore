# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# SQLite database (runtime)
*.db
*.db-wal
*.db-shm

# Environment
.env
.env.local

# Local secrets（staging 測試帳密等）——絕不進 git
*.secret.md

# PyInstaller build artifacts
build/

# Deployment artifacts (local-only)
*.dump
backups/
docker-compose.override.yml

# Local tooling
.claude/
.superpowers/

# graphify skill output (build artifact, not source)
graphify-out/

# Shioaji client runtime log
shioaji.log
*.log

# Shioaji CA 憑證檔（下單子系統 real 模式；唯讀 bind-mount 進容器，絕不進 git）
*.pfx

# OS
.DS_Store
dist/
