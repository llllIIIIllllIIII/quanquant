# 下單介面 — 使用者體驗改動需求（交付給工程師）

- 版本對象：含下單子系統的分支版本（`templates/orders.html`、`partials/*.html`、`routers/orders.py`、`static/app.css`）
- 需求來源：介面主要使用者實際操作後的回饋，逐項累積。
- **這份文件的用途**：給工程師（或工程師那邊的 Claude Code）作為實作依據。
- **本文件只放已定案的需求**。討論過程、待決事項與未來方向的技術評估留在測試端的內部紀錄，不在此。
- **關於本機參考實作**：需求提出後，我們會先在測試端的本機複本上改出效果供使用者確認。
  那份程式碼**只是效果驗證用的參考原型，不會 push、也不要求照抄**——
  請依你那邊的架構慣例自行實作，本文件的「期望行為／驗收條件」才是必須滿足的部分。

---

## 索引

| # | 標題 | 狀態 | 優先 |
|---|------|------|------|
| [001](#001--買賣改成左右分段開關並跟隨漲跌配色偏好) | 買/賣改成左右分段開關，並跟隨漲跌配色偏好 | 已確認，待工程師實作 | 高 |
| [003](#003--商品命名邏輯與顯示位置) | 商品命名邏輯與顯示位置（代碼＋中文名） | 已確認，待工程師實作 | 中 |
| [005](#005--kill-switch-控制移出下單頁下單頁只保留狀態) | Kill switch 移出下單頁 → 獨立「風險控管」頁，並更名為「緊急停止下單」 | 已確認，待工程師實作 | 高 |
| [004](#004--k-線時段選單用語調整) | K 線時段選單用語調整（全日盤／日盤／夜盤） | 已確認，待工程師實作 | 低 |
| [002](#002--下單頁要能即時看到現價並與商品欄位連動) | 下單頁要能即時看到現價，並與「商品」欄位連動 | 已確認（採 **B 版**：商品併入報價列），待工程師實作 | 高 |
| [006](#006--交易大類頁面重構下單頁精簡化委託成交未平倉獨立成頁) | 交易大類頁面重構：下單頁精簡化＋委託/成交/未平倉獨立成頁 | 已確認（使用者 2026-08-28 檢視原型），待工程師實作 | 高 |
| [007](#007--下單反饋橫幅委託回報成交回報sim-確認視窗偏好) | 下單反饋橫幅（委託回報／成交回報）＋ sim 確認視窗偏好 | 已確認（使用者 2026-08-28 檢視動態 demo），待工程師實作 | 高 |
| [008](#008--交易績效頁合併已平倉逐筆) | 交易績效頁（合併已平倉逐筆） | 已確認（2026-09-02 第三次修訂為單頁合併，經原型確認），待工程師實作 | 中 |
| [009](#009--simreal-執行模式識別有橘標模擬無標正式) | sim/real 執行模式識別：「有橘標＝模擬、無標＝正式」 | 已確認（使用者 2026-08-28 三輪比對後定案），待工程師實作 | 高 |
| [010](#010--每日復盤日層級交易日記) | 每日復盤（日層級交易日記）＋每筆改為標籤/輕量備註 | 已確認（使用者 2026-09-02 拍板），待工程師實作 | 中 |

狀態說明：`草擬中` → `本機已改，待使用者確認` → `已確認，待工程師實作` → `對方已實作`

---

<!-- 每一項改動依下列格式追加，一項一個 ## 區塊 -->
<!--
## 001 — <一句話標題>

**狀態**：草擬中　**優先**：高／中／低　**提出日期**：YYYY-MM-DD

### 使用者怎麼說
> （使用者原話，盡量不改寫）

### 現況
（現在畫面上實際發生什麼；附檔案:行號）

### 期望行為（= 驗收條件）
- [ ] （可被驗證的行為描述，不綁定實作方式）

### 不要動到的部分
（明確劃出邊界，避免對方誤改到既有機制，例如後端 mode 決策、冪等鍵、風控流程）

### 本機參考實作
- 改了哪些檔案 / 大致做法
- 截圖或前後對照

### 給工程師的實作提示
（原則性建議，例如可重用的既有元件；不強制）
-->

---

## 001 — 買/賣改成左右分段開關，並跟隨漲跌配色偏好

**狀態**：已確認（使用者 2026-08-28 確認效果無誤），待工程師實作　**優先**：高　**提出日期**：2026-08-28

### 使用者怎麼說

> 買跟賣的切換，我覺得可以加上多跟空相應的顏色區別，切換方式也可以從點選下拉，變成可以左右開關的方式。
> 然後這個功能，也可以連動設定中，可以根據使用者偏好切換的，紅漲綠跌／綠漲紅跌的功能，去切換顏色代表的意義。

### 現況

1. 「方向」是一個下拉選單（`templates/orders.html`，原第 30-32 行）：
   `<select name="action"><option value="Buy">買</option><option value="Sell">賣</option></select>`
   —— 全頁最不能點錯的欄位，視覺權重卻跟「倉別」完全一樣，而且要兩次操作（展開、選取）。
2. 沒有任何顏色區別，買賣在畫面上都是同一個灰。
3. **漲跌配色偏好在下單頁根本沒有生效**：偏好存在 `User.chart_color_scheme`（`green_up` / `red_up`，
   設定頁 `templates/account.html:19-31`），CSS 也已經有會翻轉的 token
   （`static/tokens.css`：`--rise` / `--fall` 依 `html[data-scheme]` 對調），
   但 `data-scheme` **只有儀表板那一頁會設**（`templates/dashboard.html:404` 的 inline script），
   其餘頁面（下單、交易日記、績效）都拿不到，`--rise/--fall` 永遠停在預設的綠漲。
4. 委託表格與交易日記的方向文字（`.dir.long` / `.dir.short`）用的是**固定語意**的 `--up`/`--down`，
   所以就算補上 `data-scheme` 也不會跟著翻。

### 期望行為（= 驗收條件）

- [ ] 「方向」是一個左右兩段的開關，買在左、賣在右，**一次點擊**即可切換，當前選擇一眼可辨。
- [ ] 買（做多）與賣（做空）用相對的顏色區分，選中那一側以該顏色填色，未選中維持中性灰。
- [ ] 顏色的意義跟著「帳戶設定 → K 線配色」走：
      - `green_up`（綠漲紅跌）→ 買=綠、賣=紅
      - `red_up`（紅漲綠跌，台灣慣例）→ 買=紅、賣=綠
- [ ] 切換配色偏好後，**下單頁**（不只儀表板）的買賣顏色要跟著變。
- [ ] 委託列表／部位／交易日記裡的方向文字（買/賣、多/空）與這個開關**同一套顏色語意**，不可互相矛盾。
- [ ] ~~損益數字維持固定語意：賺=綠、賠=紅，不隨漲跌配色翻轉~~ →
      **2026-08-28 使用者改訂**：損益數字（`.pnl`、績效卡片、未來的浮動損益）
      **跟隨漲跌配色偏好**——`red_up`（台灣慣例）賺=紅、賠=綠；`green_up` 賺=綠、賠=紅。
      與台灣券商軟體慣例一致，全站損益同一套語意、不分頁面。
- [ ] 不依賴 JavaScript 也能送出（表單欄位語意不變）。
- [ ] 鍵盤可操作（Tab 進入、左右方向鍵切換），螢幕閱讀器讀得出這是一組「方向」單選。

### 不要動到的部分

- 送出的欄位語意不變：仍是 `name="action"`，值仍是 `"Buy"` / `"Sell"`，**後端與既有測試不需要改**。
- 不碰後端任何邏輯：執行 mode 的 server-side 決策、`client_order_id` 冪等鍵、風控與 real 兩階段確認流程一律不動。
- `--up` / `--down` token 不要拿去做方向色（`tokens.css` 的註解已寫明兩組 token 的分工）。
  **2026-08-28 補註**：損益改隨偏好後，`--up`/`--down` 的角色收斂為**固定警示／錯誤色**
  （錯誤橫幅、緊急停止狀態等），語意不再與損益綁定；token 更名與否由工程端決定。

### 本機參考實作（僅供效果驗證，不需照抄）

| 檔案 | 做了什麼 |
|---|---|
| `templates/base.html` | `<html>` 加上 `data-scheme="{{ (u.chart_color_scheme if u else none) or 'green_up' }}"`，讓漲跌配色偏好**全站**生效，而不只儀表板。 |
| `templates/orders.html` | 「方向」的 `<select>` 換成 `<fieldset class="side-field">` + 兩個 `input[type=radio]` + `<label>` 的分段開關；`name`/`value` 完全不變。 |
| `static/app.css` | 新增 `.side-field` / `.side-toggle` 樣式（radio 視覺隱藏但保留可聚焦，`:checked + label` 上色，`:focus-visible` 有外框）；`.dir.long/.short` 由 `--up/--down` 改為 `--rise/--fall`。 |

實作重點：

- 用 **radio + fieldset/legend**，不是兩顆 button + hidden input：不靠 JS 也能送、原生就有鍵盤左右切換與單選語意。
- radio 用 `position:absolute; opacity:0` 視覺隱藏（**不是 `display:none`**），否則會失去鍵盤焦點與可及性。
- 選中色用 `color-mix(in srgb, var(--rise) 16%, transparent)` 當底色 + `inset box-shadow` 當邊框，深淺主題都不用另外寫。

預覽：`compare.html`（兩種配色並排）。實測 `red_up` 下點「賣」會得到綠色、「買」為紅色，符合台灣慣例。

### 給工程師的實作提示

- `data-scheme` 移到 base 之後，儀表板 `dashboard.html:404` 那段 inline script 就變成重複設定（值相同、無害），
  可以順手清掉，但**不要移除 `chart.js:453` 在即時切換配色時寫 `data-scheme` 的那行**——那是不重整頁面就換色的路徑。
- 若你們的設計系統有現成的 segmented control，直接用你們的；本文件只要求上面的驗收條件，不要求 class 名稱一致。
- **這一項會連帶影響交易日記**（`.dir` 是共用 class）。這是刻意的：方向色在全站必須一致，否則同一個「多」在兩頁會是不同顏色。
- 後續可考慮（尚未提出需求，先不做）：送出按鈕的文案／顏色跟著方向變、交易日記的新增交易表單也換成同一個開關。

---

## 002 — 下單頁要能即時看到現價，並與「商品」欄位連動

**狀態**：已確認（使用者 2026-08-28 選定 **B 版**），待工程師實作　**優先**：高　**提出日期**：2026-08-28

### 使用者怎麼說

> 下單頁看不到任何報價這點確實是挺不方便的。我想到的是儀表板本身就有跟現價產生連動的介面，
> 所以立即顯示價格在下單頁面，這件事是可以參考他的呈現方式的。
> 不過本身下單頁面的可以顯示範圍跟儀表板的頁面可以顯示的範圍不同，
> 然後也需要考慮到與「商品」這個選擇欄位的連動性。

### 現況

1. 下單頁沒有任何價格資訊。要掛限價單必須另開儀表板看價、記住數字、再切回來輸入。
2. 儀表板已有現成的即時報價元件可以參考／重用：
   - 片段：`templates/partials/quote.html`（現價、漲跌、量／高／低、盤別、資料時間，價格漲跌色已走 `--rise/--fall`）
   - 端點：`GET /quote`（首載）＋ `GET /quote/stream`（SSE 推播，價格有變動才帶 `flash` 做閃爍）
   - 版面：`templates/dashboard.html:3-8` 的 `.quote-panel`
3. **這兩個端點目前是「單一商品」的**：`routers/dashboard.py` 的 `quote_now` / `quote_stream` 直接讀
   `poller.last`，**完全不接受 symbol 參數**；`QuotePoller` 也只輪詢設定檔裡的那一檔。
   同時下單頁的「商品」下拉是寫死的 `symbols: ["TXF"]`（`routers/orders.py`），
   而下單白名單另有設定 `order_symbol_whitelist`（預設 `TXF`）——三處各自為政。

### 期望行為（= 驗收條件）

- [ ] 下單頁在**表單上方**即時顯示所選商品的現價、漲跌（含百分比）與資料時間，沿用儀表板同一套視覺語言。
- [ ] 這條報價列比儀表板的**更精簡**（下單頁的垂直空間要留給表單與委託列表），
      並且在**捲動到委託／部位列表時仍然看得到價格**。
- [ ] 價格漲跌色跟隨使用者的漲跌配色偏好（與 001 同一套 `--rise/--fall`）。
- [ ] **報價必須跟著「商品」欄位走**：切換商品時，報價列要換成該商品的報價。
- [ ] **若所選商品沒有報價來源，必須明確顯示「無報價」**，
      絕對不可以繼續顯示上一檔／其他商品的價格——
      「看著 A 商品的價格下 B 商品的單」比完全沒有報價更危險。
- [ ] 報價停滯／休市／疑似臨時休市的既有狀態提示（`market-banner`）在下單頁一樣要看得到。
- [ ] 手機版不因為這條報價列而把表單擠出畫面。

### 不要動到的部分

- 報價的資料來源與輪詢節奏（TAIFEX MIS 每 5 秒、`QuotePoller`、freshness/halt 判定）維持現狀，這一項只談呈現與綁定。
- 不要為了下單頁另外新增一條輪詢或第二個 poller；沿用既有的 `/quote` + `/quote/stream`。
- `partials/quote.html` 若要改，請用「加 class / 加狀態」的方式，不要改動儀表板現有的欄位與版面。

### 本機參考實作（僅供效果驗證，不需照抄）

| 檔案 | 做了什麼 |
|---|---|
| `templates/orders.html` | 標題下方加一段 `.quote-panel.quote-compact`，內容直接重用 `/quote` + `/quote/stream` + `partials/quote.html`；「商品」下拉加上 `id="order-symbol"`。 |
| `static/app.css` | 新增 `.quote-compact`：縮小 padding／字級（現價 1.75rem → 1.4rem），並 `position: sticky` 吸在 topnav 下方；手機（<768px）改回一般區塊不吸附。 |

實作重點：

- 報價區用 `hx-get="/quote" hx-include="#order-symbol"` + `hx-trigger="load, change from:#order-symbol"`，
  **切換商品時會帶著當下選取的 symbol 重抓**（不是寫死第一個商品）。後端補上 symbol 參數即可生效。
- sticky 偏移用 `top: var(--qq-topnav-h, 3.5rem)` 以 rem 表示，才會跟著 Pico 的 responsive root font-size 縮放
  （量測：1280px 寬時 topnav 69px、root 20px）。實測捲動後報價列停在 topnav 下方 1px，完全不遮擋。

### 給工程師的實作提示（重要）

1. **`/quote` 與 `/quote/stream` 需要 symbol-scoped 化**，這是本項唯一的後端工作：
   - `GET /quote?symbol=TXF` 回該商品的片段；未知／無來源的 symbol 回「無報價」狀態，不要回別檔的價。
   - SSE `/quote/stream?symbol=TXF` 只推該商品；**注意 htmx 的 SSE 擴充不會因為屬性變動就重連**，
     切換商品時要主動重建連線（例如把整個 `sse-connect` 容器換掉，或改用 `hx-on` 手動重連）。
   - 在只有單一商品的現況下，這件事可以先只做「參數收下並驗證」，行為不變。
2. **三處商品清單建議收斂成同一個來源**：`routers/orders.py` 寫死的 `["TXF"]`、
   `order_symbol_whitelist` 設定、以及 poller 追蹤的商品。否則多商品時很容易出現
   「可以下單但沒報價」或「有報價但不能下單」的組合。
3. 精簡版報價列**建議做成 `partials/quote.html` 的 class 變體**（像本機這樣加 `.quote-compact` 外層），
   而不是複製一份片段——否則之後改報價欄位要改兩個地方。
4. 尚未提出、先不做的後續：點價格直接帶入「價格」欄位、加減一檔按鈕、五檔買賣報價。

### 版面 A／B（已定案：採 B）

| | A | B |
|---|---|---|
| 商品欄位位置 | 留在表單第一格（商品／方向／口數） | **併入報價列左段**，呈現為 `TXF ▾ 46,310 ▲+246` |
| 一致性 | 版面上仍可能出現「選 A 檔、看 B 檔的價」 | 選擇器與報價是同一個元件，**結構上不可能不一致** |
| 表單第一列 | 三格 | 兩格（方向／口數），買賣開關更寬 |
| 代價 | — | 少了「商品」文字標籤；單一商品時較無感 |

**使用者選定 B**，理由：「之後產品變多的可能性極高，就算只放台指，也會有大小台甚至微台的差別，
以及當月、跨月、季的分別。」

B 版的實作重點：選擇器放在 `<form>` **外面**，用 `form="order-form"` 關聯回表單。
實測 `FormData` 仍會帶上 `symbol`（切換商品後值也正確跟著變），**送出的欄位與後端契約完全不變**。

---

## 003 — 商品命名邏輯與顯示位置

**狀態**：已確認（使用者 2026-08-28 逐項拍板），待工程師實作　**優先**：中　**提出日期**：2026-08-28

### 使用者怎麼說

> 針對這部分我還有想調整的，也就是商品名稱的放置，我覺得可以放在我圈選的這個地方
> （＝報價列中，商品選擇器與現價之間），同時儀表板的部分也可以做相應的調整。
> TXF 的稱呼應該會是台指期，但是實際上在下單的時候，券商可能會用「台指近」這種稱呼，
> 我們也可以思考一下命名邏輯。
>
> （後續補充）以目前該商品的設定的話，我覺得比較接近「台指近」。

### 現況

- 全站只顯示代碼：報價列 `TXF`、圖表標題 `TXF`、委託列表與部位表也都是 `TXF`。
- 沒有任何地方顯示中文品名，也沒有顯示**契約月**——但資料裡其實有
  （`models.py` 的 `FuturesSnapshot.contract_month`，例如 `"202609"`）。

### 命名邏輯（本項的核心結論）

顯示名拆成三層，**各有各的用途，不可互相取代**：

| 層 | 例 | 用途 |
|---|---|---|
| 商品代碼 | `TXF` / `MXF` / `TMF` | 系統內部、API、**送單標的的唯一依據**。畫面上不可省略。 |
| 中文品名簡稱 | 台指 / 小台 / 微台 | 人看的辨識，對應交易者口語。 |
| 期別 | 近（目前恆為近月）／次／季／具體月份 | 指出是哪一個契約。 |

**顯示名 = 品名 + 期別（+ 契約月）** → `台指近 2609`

「近」目前是正確且穩定的說法，有程式面的依據：`sources/taifex.py` 的 `_select_nearest_contract`
一律挑**最近到期**的合約（且會依日盤／夜盤選 `-F`／`-M`）。也就是說系統顯示與送出的本來就
永遠是近月，稱「台指近」名實相符。

但也因為換月是自動發生的，**契約月必須看得見**：換月當天標的其實已經換過一次，
畫面上若只有「台指近」，使用者不會察覺。

### 期望行為（= 驗收條件）

- [ ] 報價列在「商品選擇器」與「現價」之間顯示中文顯示名（`台指近`）與契約月（`2609`）。
- [ ] 儀表板（共用同一個報價片段）同步顯示，不需要另外維護一份。
- [ ] 未知／未對映的商品代碼，**顯示代碼本身**，不可顯示錯誤的中文名或空白。
- [ ] 中文名只用於顯示；送單、API、資料表的 key 一律仍是代碼。
- [ ] 換月後契約月的數字要跟著變（不需人工維護）。

### 不要動到的部分

- 送單 payload 的 `symbol` 仍是代碼（`TXF`），**不可改成中文名**。
- `FuturesSnapshot` 既有欄位不動；`contract_month` 本來就有，只是沒被顯示。
- 委託列表／部位表目前顯示代碼，本項暫不更動（見下方待決事項）。

### 本機參考實作（僅供效果驗證，不需照抄）

| 檔案 | 做了什麼 |
|---|---|
| `web/templating.py` | 新增 `SYMBOL_LABELS = {"TXF": "台指", "MXF": "小台", "TMF": "微台"}` 與 `symbol_label` filter（未知代碼回代碼本身）。 |
| `templates/partials/quote.html` | `.sym` 拆成 `.sym-code`（代碼）與 `.sym-name`（品名＋期別＋契約月）。 |
| `templates/dashboard.html` | 圖表標題加上中文顯示名。 |
| `static/app.css` | `.sym-code` / `.sym-name` 樣式；合併版報價列只隱藏 `.sym-code`（代碼已由選擇器呈現）。 |

結果：下單頁讀作 `TXF ▾ ｜ 台指近 2609 ｜ 46,310 ▲ +246 (+0.53%)`。

### 給工程師的實作提示

- 對映表建議與下單白名單、poller 商品清單**同源**（見附錄 A 第 4 點），否則會出現
  「能下單但沒有中文名」這種不一致。
- 未來支援指定契約月時，「近」不能再寫死：期別應由**實際選到的契約**決定
  （近／次／季，或直接顯示 `台指2512`）。本機實作把這件事寫在模板註解裡了。
- 若你們有既有的商品/契約 registry，直接用它，不必照抄這張對映表。

### 時段（日盤／夜盤／全日盤）要不要進名稱？

使用者提問：券商慣稱有「台指近全」（全時段），我們是不是也該叫「台指近全」？

**結論：分開處理，規則是「名稱裡只放使用者能選的維度；不能選的維度用狀態顯示」。**

| 位置 | 顯示名 | 理由 |
|---|---|---|
| 下單頁 | `台指近`（**不加「全」**） | 下單標的就是近月這一個契約，日盤／夜盤是交易時段、不是不同標的。加「全」會暗示還有「台指近日」可選，但實際上沒有這個選項。時段狀態已由報價列右側的即時盤別徽章（● 日盤／夜盤）表達。 |
| 儀表板 K 線 | `台指近全` / `台指近日` / `台指近夜` | 這裡的時段**是使用者可選的**（圖表時段選單：全部／只日盤／只夜盤，預設全部），後綴要跟著選擇變。 |

佐證：`sources/taifex.py::_select_nearest_contract` 會依當下時段自動選日盤（`-F`）或夜盤（`-M`）
序列，因此我們的報價本來就是全時段涵蓋；而 `static/chart.js` 的 `sessions` 已有
`all / day / night` 三個選項、預設 `all`。

本機參考實作：`dashboard.html` 的圖表標題改為 Alpine `x-text`，後綴跟著 `session` 變動
（`all→全`、`day→日`、`night→夜`）；下單頁維持 `台指近`。

### 契約月（`2609`）該不該顯示？

使用者提問：旁邊的 `2609` 是否多此一舉？

**結論：目前必須保留，但理由不是「提醒行情跳空」，而是資料模型的缺口（見附錄 A 第 7 點）。**

- `broker/types.py::Position` 與 `db/models.py::Order`、`BrokerPosition` **都只有 `symbol`，沒有契約月**。
- 部位的唯一鍵是 `(user, broker, account, mode, symbol, direction)` —— 換月後在新契約下的單，
  系統會與舊契約的部位**當成同一個部位淨額計算**，但券商端是兩個不同契約、不會互相平倉。
- 在資料模型補上契約月之前，畫面上的契約月是**唯一能讓人察覺換月的線索**。

**使用者決定：不在報價列顯示契約月**，顯示名維持 `台指近`。換月的察覺不由這條報價列負責。

換月的察覺由其他機制負責，不屬於這條報價列的職責。

### 各顯示位置的定案

| 位置 | 顯示 | 決定理由 |
|---|---|---|
| 報價列（下單頁） | `TXF ▾ ｜ 台指近 ｜ 46,310` | 代碼在選擇器、中文名在旁邊 |
| 商品下拉（收合） | 只有代碼 `TXF` | 保持緊湊，且與旁邊的中文名不重複 |
| 商品下拉（展開） | 中文名為群組標題、代碼為選項 | 不熟代碼的人不用背；**用原生 `<optgroup>` 達成，不需要自訂下拉元件** |
| 委託列表／部位表的「商品」欄 | **維持代碼** | 欄位窄、對帳一律以代碼為準；中文名在上方報價列已出現過 |
| 儀表板圖表標題 | `台指近全／台指近日／台指近夜` | 跟著時段選單變（該處時段是使用者可選的維度） |

`<optgroup>` 的關鍵性質：**群組標題只在展開的清單中出現，收合狀態顯示的仍然只有 `<option>` 的文字**。
因此「展開看中文名、收合只看代碼」不需要自訂元件——鍵盤操作、螢幕閱讀器、手機的原生選單全部保留。
（自訂下拉的成本不在載入量，而在要自己重做這三件事，尤其手機的原生選單體驗無法複製。）

未來若同一商品要選不同契約月，`<optgroup>` 會自然變成真正的分組（`台指近` → `TXF202609` / `TXF202512`），
語意比現在更貼切。

---

## 004 — K 線時段選單用語調整

**狀態**：已確認，待工程師實作　**優先**：低　**提出日期**：2026-08-28

### 使用者怎麼說

> 那個「只日盤」「只夜盤」的「只」，我覺得閱讀起來有點怪，我想調整一下選單的詞。

### 現況

儀表板 K 線的時段選單（`static/chart.js` 的 `sessions`）是：`全部` / `只日盤` / `只夜盤`。

### 期望行為（= 驗收條件）

- [ ] 選單用語改為交易者本來使用的盤別名稱：

  | 原本 | 改成 |
  |---|---|
  | 全部 | **全日盤** |
  | 只日盤 | **日盤** |
  | 只夜盤 | **夜盤** |

- [ ] 選項的 `code`（`all` / `day` / `night`）與任何 API 參數（`session_mode`）**不變**，只改顯示字串。
- [ ] 與 003 的圖表標題後綴（`台指近全／台指近日／台指近夜`）對應一致。

### 不要動到的部分

- `session_mode` 的值與 `/api/candles` 的查詢參數維持 `all` / `day` / `night`。
- 時段的資料過濾邏輯（`candles/service.py::_session_filter`）不動。

### 給工程師的實作提示

「只」讀起來怪，是因為那是篩選器的口吻（「只顯示…」），但這三個選項其實是台指期本來就有的盤別名稱。
改用名稱之後，選單與標題後綴的語彙才會一致。

---

## 005 — Kill switch 移出下單頁 → 獨立「風險控管」頁，並更名為「緊急停止下單」

**狀態**：已確認（使用者 2026-08-28 決定落點與命名），待工程師實作　**優先**：高　**提出日期**：2026-08-28

### 使用者怎麼說

> 在下單頁有一列「下單正常／啟動 Kill switch 拒絕下單」。我覺得它的位置不太應該出現在下單的介面，
> 除了影響視覺之外（特別是它還是白色，最搶眼），也不是一個適合放在該頁面的功能。
> ……它更偏向我構想的「交易冷靜期」功能（我覺得也可以把現在的名字改掉），
> 更適合放在設定頁面、或甚至獨立出來一個風險控管頁面。

### 現況

- `templates/orders.html` 直接 `include` 了 `partials/kill_switch_control.html`（owner 才顯示）。
- 那顆按鈕用 Pico 的 `contrast` 樣式 → 深色主題下是**白底黑字**，成為整個下單頁視覺最強的元素，
  比「送出」還搶眼。
- 它佔掉表單正上方一整列，把報價、表單、委託列表整個往下推。
- kill switch 開啟時，下單頁**沒有任何提示**：使用者填完表單、按下送出，才會收到被拒絕的錯誤。

### 期望行為（= 驗收條件）

- [ ] 下單頁**不再有 kill switch 的控制項**（開關本身移到風控設定的位置）。
- [ ] 下單頁在**停止狀態下必須明確顯示狀態**：說明目前不接受新單、並提供前往解除的入口。
- [ ] 停止狀態下「送出」按鈕直接停用——不可以讓使用者填完、按下去才發現送不出。
- [ ] 正常狀態下不佔任何版面（不顯示「下單正常」這種常態訊息）。
- [ ] 控制項移動後，owner 仍能在新位置即時切換，行為與現在完全一致。
- [ ] 控制項落在**獨立的「風險控管」頁面**，並在主導覽列有入口。
- [ ] 功能更名為**「緊急停止下單」**（畫面上不再出現 "Kill switch" 字樣）。
- [ ] 該頁需說明這個開關的實際行為：**啟動只擋新單、不會自動取消既有掛單**（現行後端行為）。
- [ ] 非 owner 進入該頁可以看到目前狀態，但沒有切換控制。

### 不要動到的部分

- **後端行為完全不變**：`POST /orders/kill-switch` 端點、`assert_owner` 授權、runtime 即時生效、
  「翻開只擋新單、不自動撤既有掛單」的設計、以及切換時的 ops 告警（含未成交掛單數）一律照舊。
  本項只動「控制項出現在哪個畫面」與「下單頁如何呈現狀態」。
- 不要因為移走控制項就把狀態也一起藏起來——停止狀態必須在下單頁看得見。

### 本機參考實作（僅供效果驗證，不需照抄）

| 檔案 | 做了什麼 |
|---|---|
| `templates/orders.html` | 移除 `kill_switch_control.html` 的 include；改為只在 `kill_switch` 為真時顯示紅色狀態橫幅（連結到 `/risk`），並在該狀態下 `disabled` 送出按鈕。 |
| `templates/risk.html`（新） | 風險控管頁：一張「緊急停止下單」卡片，含行為說明＋原本的控制片段；非 owner 只顯示狀態。 |
| `templates/partials/kill_switch_control.html` | 文案更名：`Kill switch` → `緊急停止`（含啟動／解除／停用三種狀態的字串）。 |
| `templates/base.html` | 主導覽列加入「風險控管」入口。 |
| `web/routers/orders.py` | 新增 `GET /risk`；沿用既有的 `risk_guard` 相依與 owner 判定。切換端點仍是 `POST /orders/kill-switch`，契約未變。 |

移除後的效果：下單頁從「報價 → 表單 → 送出」變成**一屏內看得完**（原本被那一列推到畫面外）。

導覽入口的位置：放在**主導覽列**而非使用者選單，因為緊急停止是「出事時要最快按到」的東西，
多一層下拉會拖慢。若日後覺得太少用，改放使用者選單只是搬一行。

`GET /risk` 放在 `routers/orders.py` 只是因為 `risk_guard` 的相依與授權判定都已在該檔；
獨立成 `routers/risk.py` 完全可以，端點契約不受影響。

### 使用者的決定（2026-08-28）

1. **落點：獨立的「風險控管」頁面**（不是併進帳戶設定頁）。理由：使用者規劃中的風控功能
   （交易冷靜期、自我監控提醒、操作摩擦機制）之後都會落在這一頁，先把緊急停止放進去卡位。
2. **命名：「緊急停止下單」**，交易冷靜期另開為獨立功能（見下方設計觀點）。

### 設計觀點：Kill switch 與「交易冷靜期」不該合併（節錄，完整見風控文件）

使用者提到現在的 kill switch「更偏向交易冷靜期」。實際比對兩者的語意後，建議**兩個功能並存**：

| | 現行 Kill switch | 交易冷靜期（使用者構想） |
|---|---|---|
| 目的 | 緊急煞車（系統／行情異常） | 自我約束（防衝動交易） |
| 誰啟動 | owner | 使用者自己 |
| 解除 | **必須能隨時解除**（狀況排除後要立刻恢復交易） | **刻意不可提前解除**（能一鍵取消就失去意義） |
| 建議名稱 | 緊急停止下單 | 交易冷靜期 |

兩者的「解除」語意剛好相反，合併成同一個功能必定犧牲其中一邊。建議放在同一個風控頁面、
但維持為兩個獨立的開關。

---

## 006 — 交易大類頁面重構：下單頁精簡化＋委託/成交/未平倉獨立成頁

**狀態**：已確認（使用者 2026-08-28 檢視本機原型後確認），待工程師實作　**優先**：高　**提出日期**：2026-08-28

### 使用者怎麼說

> 委託跟倉位目前還在下單頁面……沒有到不好，但是整體畫面很亂，所以是否應該要分成一個
> 「交易的大類」然後「下單」「委託」「成交」「未平倉」「已平倉」的這幾個頁面。
>
> 下單頁，可以有「目前的未平倉部位顯示以及目前浮動損益」（只要短短的一小區就好，不是一個頁面，
> 而且這個是對應商品的，也就是在切換商品後，如果沒有持有該商品，就沒有顯示），
> 以及目前尚有的保證金數字（是可動用保證金的數字）。
> 目前的委託跟成交那塊可以直接移走沒有問題。

### 現況

- `templates/orders.html` 一頁塞了：報價列＋表單＋mode 檢視分頁＋委託表＋部位表＋改單 modal＋agent 狀態。
- 部位表只有 商品/方向/口數/均價，沒有浮動損益；頁面上沒有任何保證金資訊。
- 逐筆成交其實已有獨立資料表（`db/models.py::Deal`，fill_id 去重、含 fee），只是沒有畫面。

### 期望行為（= 驗收條件）

**下單頁（精簡化）**：

- [ ] 下單頁只保留：報價列（002）、下單表單、**部位/浮損/保證金精簡條**、agent 狀態、
      緊急停止狀態橫幅（005）。委託表、部位表、mode 檢視分頁、改單 modal 全部移出。
- [ ] 精簡條是**一小條**（單列），不是表格；內容三段：
      「未平倉 多 N 口 @均價」「浮動損益 ±X」「可動用保證金 Y」。
- [ ] 精簡條是 **symbol-scoped**：跟著商品選擇器走；切換商品後若未持有該商品，
      部位與浮損兩段**整段不顯示**（不是顯示 0）。
- [ ] 浮動損益＝未實現損益（方向調整後點數差 × 口數 × 點值），隨報價即時更新；
      顏色跟隨紅綠偏好（001 改訂：全站損益隨偏好——red_up 賺=紅、green_up 賺=綠）。
- [ ] 可動用保證金為帳戶層級：real 取券商保證金 API；**sim 在虛擬保證金功能上線前
      顯示中性占位（如「—」）**，不可顯示錯誤數字。
- [ ] 精簡條在下單/成交/部位變動（SSE）與商品切換時刷新。

**交易大類（表格的去處）**：

- [ ] 主導覽出現「交易」大類，其下（或同層）至少包含：下單、**委託**、**成交**、**未平倉**。
- [ ] 「委託」頁承接原委託表的**全部既有功能**：mode 檢視分頁（P0-1 的選中態要修好）、
      改單 modal、取消——功能與後端契約不變，只是搬家。
- [ ] 「成交」頁顯示逐筆成交（時間、商品、方向、口數、價格、手續費），資料源 `Deal` 表。
- [ ] 「未平倉」頁承接原部位表，並補上浮動損益欄（同精簡條的計算）。
- [ ] 「交易績效」頁不在本項範圍——見 **008**（2026-09-02 定案：績效與已平倉逐筆**合併為單頁**）。

### 不要動到的部分

- 下單表單本身（001 的買賣開關、002 的報價列）與送出契約不變。
- 委託的改單/取消端點、SSE `orders-changed` 機制、agent 狀態判定不變——本項是「搬與加」，不是改流程。
- 浮損計算請重用 `journal/pnl.py::unrealized_pnl`，不要另寫一套數學。

### 本機參考實作（僅供效果驗證，不需照抄）

| 檔案 | 做了什麼 |
|---|---|
| `templates/partials/position_strip.html`（新） | 精簡條片段：pos 為 None 時部位/浮損段不 render；margin 為 None 時顯示占位 |
| `templates/orders.html` | 表格/mode 分頁/改單 modal 移出；加 `#pos-strip-box`（hx-get ＋ load/商品 change/sse 刷新） |
| `static/app.css` | `.pos-strip` 樣式；`.pnl` 改用 `--rise/--fall`（001 改訂的實作） |

效果：下單頁「報價 → 精簡條 → 表單 → 送出」一屏內看完。預覽 `index-red_up.html`（有倉＋紅漲綠跌）、
`index-multi.html`（無倉隱藏＋sim 占位）。

### 給工程師的實作提示

- 精簡條建議做成 hx-get 片段端點（如 `GET /orders/position-strip?symbol=`），
  注意它需要位於 `sse-connect` 作用域內（或自帶連線）`sse:orders-changed` 才收得到。
- 浮損的「隨報價更新」可先掛在既有 quote SSE 的節奏上（同一個 5 秒源），不必新增推播。
- real 保證金：Shioaji 有 margin 查詢；欄位選擇與快取頻率你們定（F4 那邊也用得到，建議一次接好）。
- 導覽結構（「交易」做下拉還是平鋪子頁籤）依你們 topnav 慣例自行決定，驗收只看「找得到、點得到」。
- 委託頁搬家時，P0 清單裡的 8（欄位不足）、9（狀態英文）、10（價格格式）、11（table-wrap）
  建議順手一起修——反正整張表要重寫一次版面。

---

## 007 — 下單反饋橫幅（委託回報／成交回報）＋ sim 確認視窗偏好

**狀態**：已確認（使用者 2026-08-28 檢視動態 demo 後確認），待工程師實作　**優先**：高　**提出日期**：2026-08-28

### 使用者怎麼說

> 不論成功委託或是失敗都要在畫面上跳出來……我的想像是手機 app 的成功委託通知，
> 一個從右到左滑動的橫幅，不會佔據畫面太多空間（約 1/10），通常會在最上面，
> 顯示委託的成功與否還有委託的商品的口數，這個橫幅可以是灰色的（中性的）。
>
> 成交也要跳出橫幅，並且可以分成紅綠（下單的方向，這也牽涉到使用者偏好），
> 一樣需要揭露成交的資訊，包含在什麼價格、成交幾口。市價單有可能滑價，
> 用不同的價格去成交，就乾脆一個價格一次橫幅去分（我覺得比較好）。

### 現況

- 下單成功完全沒有可見回饋（`_place_success()` 只換冪等鍵＋觸發列表刷新，見初步觀察 P0-3）；
  006 把委託表移走後，回饋缺口更大——橫幅是委託表搬家的**前置補償**，兩項應一起上線。
- 錯誤走 `.form-error-slot` 紅色 banner，成功不清除（P0-4）、取消失敗看不到（P0-5）。
- 專案已有 `.toast-stack`/`.alert-toast`（儀表板告警）與 SSE 管線可延伸。

### 期望行為（= 驗收條件）

**委託回報橫幅**（成功與失敗都必須跳）：

- [ ] 從畫面右側滑入、頂部堆疊、約一列高（不佔版面），數秒後自動滑出，點擊可提前關閉。
- [ ] 成功：中性配色（有別於頁面背景的灰），內容含 商品、方向、口數、價格。
- [ ] 失敗：**固定警示紅（`--down`）實心底＋粗框＋✕圖示＋「委託失敗」標題**＋失敗原因。
      不用方向色系——失敗必須與成交橫幅一眼可辨（型式＋圖示＋文字，不只靠色相；兼顧色弱）。
- [ ] 改單、取消的成敗回饋走同一套橫幅（一併解決 P0-4／P0-5／P0-6 的回饋缺口）。

**成交回報橫幅**：

- [ ] 左側粗邊（或等效視覺）用**下單方向的顏色**，跟隨紅綠偏好（--rise/--fall，同 001 語意）。
- [ ] 內容含 商品、方向、**成交口數與成交價格**。
- [ ] **一個成交價格一條橫幅**：市價單滑價分成多價成交時，逐筆各跳一條（`Deal` 逐筆存，天然對齊）。
- [ ] 有堆疊上限與收合行為（原型參數：上限 5 條、成交停留 5 秒、失敗 8 秒——可調，但失敗要比成功停得久）。
- [ ] 橫幅容器帶 `aria-live="polite"`，螢幕閱讀器唸得到（順帶補 P2-22 的缺口）。
- [ ] `prefers-reduced-motion` 時停用滑入動畫（直接出現/消失）。

**sim 下單確認視窗**：

- [ ] sim 送出前預設跳出確認視窗（內容同 real 確認框的完整委託摘要，015 的要求一致）。
- [ ] 視窗上可勾「**不再顯示**」，勾選即存為使用者偏好，之後 sim 直接送出。
- [ ] real 的兩階段確認為後端強制，**完全不受此偏好影響**。
- [ ] （預留）風控功能 F5「強制二次確認」開啟時，此偏好被鎖定為必跳。

### 不要動到的部分

- 表單欄位驗證錯誤（打錯價格之類）仍就地顯示在表單旁，不進橫幅——橫幅只給「送出之後」的回報。
- 冪等鍵、送出流程、real 兩階段確認的後端契約一律不變。
- 既有儀表板告警 toast 不動；橫幅可共用樣式基底但事件來源分開。

### 本機參考實作（僅供效果驗證，不需照抄）

| 檔案 | 做了什麼 |
|---|---|
| `static/app.css` | `.banner-stack`（fixed 右上堆疊）＋`.order-banner`（基底中性）＋`.fail`／`.fill-long`／`.fill-short` 變體＋滑入/滑出 keyframes＋reduced-motion 降級 |
| 預覽 `banners.html` | 純前端 demo：六顆按鈕試所有變體、滑價三連發、紅綠偏好即時切換（使用者已按過並確認效果） |

### 給工程師的實作提示

- **事件來源是本項唯一的後端工作**：現行 SSE 只推汎用 `orders-changed`（叫前端重抓），
  橫幅需要**帶內容的事件**（委託回報／逐筆成交回報各一種，payload 含商品/方向/口數/價格/成敗原因）。
  建議在 `/orders/stream` 加事件型別，源頭是 RawInboxWorker 落地時。注意：橫幅事件只發給
  **下單當事人**（user-scoped），不是廣播。
- 滑價「一價一橫幅」＝一筆 `Deal` 一個事件，落地層天然逐筆，不需要聚合再拆。
- 確認視窗偏好建議存 `User` 上（跨裝置一致），不要放 localStorage。
- 失敗原因文案請用人話（「超過單筆上限 5 口」而不是 exception 字串）——這與 P1-13 的
  風控透明化相關，至少拒單原因要能直接讀懂。

---

## 008 — 交易績效頁（合併已平倉逐筆）

**狀態**：已確認（2026-09-02 **第三次修訂：兩頁分立 → 單頁合併**，經本機原型比對後定案），
待工程師實作　**優先**：中　**提出日期**：2026-08-28（2026-09-02 改寫）

### 使用者怎麼說（2026-09-02）

> 可以試試看把績效統計的頁面跟之前提到的已平倉做在一起，如果要這樣的話，需要討論的就是
> 如何把績效統計現有的巨大 UI，縮減到有辦法看到一筆一筆交易的狀態，然後預設一樣是今日交易
> （交易日），顯示的內容會隨著設定切換是否為模擬交易或是實單而定。
>
> 這樣子就能在一個頁面，去檢核今日的交易績效，也可以逐筆盤查（如果想要檢視更大週期的績效，
> 也可以用上方的篩選功能去過濾）。

### 修訂沿革（設計紀錄，保留勿刪）

1. 2026-08-28 初版：績效頁升級承接已平倉明細（單頁）。
2. 同日修訂：改為**兩頁分立**——已平倉是輕量對帳頁，績效頁要留未來統計內容的擴充空間。
3. **2026-09-02 定案：改回單頁合併**。轉折的原因是使用者看到實際頁面後發現
   **九張大統計卡片吃掉整個畫面、一筆交易都看不到**——先前擔心的「合併會太擠」，
   真正的病灶其實是卡片沒有分層。把指標依「單日相關／區間統計」分層壓縮之後，
   單頁同時容納摘要與逐筆完全足夠，也就不需要拆兩頁。

### 現況

- 績效頁（`stats.html`）版面：mode 分頁 → 篩選列（**套用鈕整條全寬，比任何數字都搶眼**）
  → 一行說明 → **九張大卡片（兩列）** → 依標籤／依商品兩張分組表。
- 日期篩選預設**空白＝全期間**，不是今日；沒有任何逐筆交易。
- 九個指標混了兩種性質：總損益／交易次數／勝率是「今天做得怎樣」；
  平均獲利、平均虧損、最大單筆獲利、最大單筆虧損、最大回撤、獲利因子是**區間統計**——
  單日只有 3 筆交易時，最大回撤與獲利因子幾乎沒有意義。

### 期望行為（= 驗收條件）

**頁面歸屬與命名**

- [ ] 併入「交易」大類，命名 **「交易績效」**；原獨立「績效統計」導覽項退場。
- [ ] 本頁同時承擔統計摘要與**已平倉逐筆明細**，不另立已平倉頁。

**指標分層（本項的核心）**

- [ ] **常駐一列**（不使用大卡片）：總損益（最大字級）、交易次數、勝率（附 N 勝／N 負）。
- [ ] **收進「更多指標」摺疊**：平均獲利、平均虧損、最大單筆獲利、最大單筆虧損、
      最大回撤、獲利因子，**以及依標籤／依商品兩張分組表**。
- [ ] 摺疊區的預設狀態**跟著區間走**：選「今日」時收合；區間大於單日（本週／本月／自訂跨日）
      時**自動展開**——那才是區間統計有意義的時候。使用者手動收合／展開的結果以當次為準。
- [ ] 壓縮後，一般桌面視窗（約 1440×900）在「今日」狀態下應能**同時看到摘要與至少 8 筆交易**，
      不需捲動。

**篩選列**

- [ ] 壓成一行；**套用鈕恢復正常尺寸**（不可全寬）。
- [ ] **快捷區間**：今日／本週／本月／自訂（自訂才顯示起訖日期欄）。
- [ ] 預設 **今日**，以 `trading_day` 定義（**含前一夜盤**，昨晚的夜盤單算今日）。
- [ ] 既有篩選保留：商品、標籤。
- [ ] **新增「結果」**：全部／只看獲利／只看虧損——長區間複盤時「只看輸的那些」是主要動線。
- [ ] **新增「含手動補記」勾選**：預設不勾＝只計 `source="shioaji"`（券商自動回寫，
      即「在 quanquant 內完成的交易」）；勾選後納入 `source="manual"`。
- [ ] **sim 輪次**：sim 模式時出現，預設「第 N 輪（當前）」，可切歷史輪或改用日期區間；
      real 無此欄位。（輪次模型見 `2026-08-28-sim-account-features.md`）
- [ ] mode（正式／模擬）切換沿用共用元件，見 **R2-10**（round2 文件）。

**已平倉逐筆明細**

- [ ] 欄位：平倉時間、商品、方向、口數、進場均價、出場均價、損益、**來源標記**（自動／手動）、
      **標籤・備註**。
- [ ] **標籤・備註欄的設計（2026-09-02 修訂，原為每列一個「寫復盤」連結）**：
      顯示該筆的標籤 chips（可快速加標）＋一個**低調的備註鈕**（有寫才亮、沒寫 hover 才出現）。
      **不要**在每一列放顯眼的「寫復盤」CTA——那會暗示「每筆都該寫散文」，
      實際上寫不完（見 010 的設計理由），最後整個日記系統會被放棄。
- [ ] 方向與損益顏色跟隨漲跌配色偏好（001 改訂）。
- [ ] 資料源為 `Trade` 表（round-trip 完成時由 `BrokerPosition` 回寫 `Trade(source="shioaji")`，
      與手動補記同表）——**不要另外從 BrokerPosition 再組一份**。
- [ ] **分頁**：每頁固定筆數（建議 50）＋「共 N 筆、第 X/Y 頁」。
      **不可**用「固定上限＋截斷」（現行委託列表靜默取 100 筆已是缺陷 P2-25，不要複製）。

**⚠ 實作陷阱（務必處理）**

- [ ] **統計數字必須涵蓋整個篩選區間，不是只算當前頁**。上方摘要與「更多指標」都是
      區間彙總；分頁只影響下方表格的顯示。這點寫錯的話，翻頁時總損益會跟著變，
      而且不會有人立刻發現。

### 不要動到的部分

- 統計數學（`stats/metrics.py`）不動——本項改的是資料切片（來源／輪次／區間）與版面。
- `Trade` 資料模型不動（`source` 欄位已存在）。
- 匯出 CSV／Excel 維持，且**匯出範圍應與當下篩選一致**（不受分頁影響）。

### 本機參考實作（僅供效果驗證，不需照抄）

| 檔案 | 內容 |
|---|---|
| `screenshots-target/07-交易績效-今日-目標.png` | 今日狀態：篩選一行＋指標一行＋八筆交易一屏內看完 |
| `screenshots-target/08-交易績效-本週展開-目標.png` | 區間＞單日：「更多指標」自動展開（六項進階＋兩張分組表）後的樣子 |

原型為靜態頁，互動（區間切換連動摺疊）以 JS 模擬，正式實作請照你們的 HTMX 慣例。

### 給工程師的實作提示

- 摺疊建議用原生 `<details>` 或既有 Alpine 慣例；「區間＞單日自動展開」是**伺服器端就能決定
  的初始狀態**（依 date_from/date_to 是否同一交易日），不必等前端 JS。
- 快捷區間建議只是「幫使用者填好 date_from/date_to 並標記 active」，
  後端仍只吃日期區間，不必新增區間語意的參數。
- 「結果」與「含手動補記」都是既有查詢的 WHERE 條件，`stats.py::_filtered` 加參數即可。
- 分頁與統計要走**兩個查詢**（彙總不分頁、明細分頁），別為了省一次查詢而讓統計跟著分頁。

---

## 009 — sim/real 執行模式識別：「有橘標＝模擬、無標＝正式」

**狀態**：已確認（使用者 2026-08-28 經三輪原型比對後定案），待工程師實作　**優先**：高　**提出日期**：2026-08-28

### 使用者怎麼說

> （第二輪，推翻警示紅方案）我覺得，反而，模擬單可以用橘黃色這種比較明顯的顏色，
> 而真單可以用灰色……也不用寫「本次下單」這麼長的字，只要寫模擬單、正式單就好。
>
> （定案）我個人是希望有無橘標去做模擬與正式的分別就好，會比較正式一點。

### 現況

- 下單頁沒有任何執行 mode 標示（初步觀察 P0-2）。執行 mode 是 server 決定的
  （`app.state.order_service.mode`）；`?mode=` 只是檢視參數，且已隨委託列表移出下單頁（006）。
- 使用者唯一的線索是 real 下單會跳兩階段確認——太間接、且發生在按下送出之後。

### 期望行為（= 驗收條件）

- [ ] **sim 執行模式**：下單頁標題旁顯示**橘黃「模擬單」徽章**（amber 系，
      同夜盤徽章／`badge.open` 的 `#f0b90b` 色族）。
- [ ] **real 執行模式**：**不顯示任何 mode 徽章**，頁面保持乾淨。
- [ ] 徽章反映 **server 執行 mode**，與任何 `?mode=` 檢視參數無關。
- [ ] **real 兩階段確認框必須明示「正式單」**並帶完整委託內容（與初步觀察 P1-15 的
      確認框補完整內容一併處理）——「無標＝真錢」的辨識弱點由確認框在送出前補強。
- [ ] 007 的 sim 確認視窗同樣明示「模擬單」。
- [ ] 警示紅（`--down`）**不得**用於 mode 識別——專用於錯誤／緊急停止（既有決策）。
- [ ] 委託／成交／未平倉／交易績效等頁的 sim/real 分頁屬「檢視」語意，不受本項影響。

### 不要動到的部分

- 執行 mode 的 server-side 決策機制不變；本項純呈現層。
- real 兩階段確認流程不變（只補顯示內容）。

### 設計紀錄（三輪演進的取捨，保留勿刪）

1. 第一版（測試端提案）：real＝警示紅三重訊號（紅徽章＋表單紅框＋紅送出鈕）。
   被使用者推翻——警示紅太吵，且與紅漲綠跌下的方向紅有混淆疑慮。
2. 第二版：配色反轉為「**標亮特殊環境**」——模擬單橘黃、正式單中性（灰／淡紫兩候選）。
   邏輯：正式是常態、模擬才是要被標記的狀態（同各類測試環境掛橘標的業界慣例）；
   「誤把正式當模擬」零損失，「誤把模擬當正式」另有 real 兩階段確認把關。
3. 定案（使用者）：**real 連中性徽章都不掛**——「有無橘標」就是全部的區別，更正式。
   已知弱點：「以標記的**缺席**當訊號」比「標記的存在」弱（徽章沒渲染、使用者沒注意
   都會誤讀為 real）——補強即驗收條件裡的「real 確認框明示單別」，把辨識的最後一道
   放在送出前的必經路徑上。

### 本機參考實作（僅供效果驗證，不需照抄）

| 檔案 | 做了什麼 |
|---|---|
| `templates/orders.html` | 標題旁 `{% if exec_mode != 'real' %}` 的「模擬單」徽章；real 無輸出 |
| `static/app.css` | `.exec-mode.sim`（#f0b90b 16% 底＋同色字框）；無 real 樣式 |

預覽：`index.html`（sim 橘標）vs `index-real.html`（real 無標）。

---

## 010 — 每日復盤（日層級交易日記）

**狀態**：已確認（使用者 2026-09-02 拍板），待工程師實作　**優先**：中　**提出日期**：2026-09-02

### 使用者怎麼說

> 有一個「日記」欄位，要寫復盤的，這個是我們要的嗎？假設是同一天，那你覺得日記要怎麼寫？
> 以專業操盤手的習慣，真的會這樣，一筆一筆寫交易日記復盤嗎？
> 然後這些交易日記的內容要怎麼整理你有想過嗎？

### 設計理由（本項的核心，設計紀錄保留勿刪）

**專業交易者不會每筆寫散文。**實務上的分層是：

| 層 | 內容 | 性質 |
|---|---|---|
| 每筆 | 標籤等**結構化標記** | 快、且**會累積成統計** |
| 少數筆 | 真正值得的文字評註（最大虧損、破戒的那筆） | 8 筆裡通常 1–2 筆 |
| **每日／盤別** | 紀律、情緒、明天調整——**反省真正發生的地方** | 目前**完全不存在** |

台指當沖一天 8 筆，若每筆都要寫散文＝每天 30 分鐘以上的低價值書寫，**兩週內必然放棄**，
且產出高度重複（「突破進場／停利出場」×8），回頭看沒有資訊量。

**關鍵洞見：散文不能被統計，標籤可以。** 而本系統**已經有「依標籤」的績效分組表**——
這條迴路才是日記真正的回報：替幾筆標「追高」→ 月統計顯示該標籤總損益 −18,000 →
**壞習慣被量化出價格**。這比一百篇文字反省有用，而且自動長出來。

因此：每筆維持輕量結構化標記（008 已修訂），**新增日層級的復盤實體**——
而且它與使用者整套風控構想（過度交易、情緒失控、冷靜期）是同一個層級的東西。

### 期望行為（= 驗收條件）

- [ ] 新增**每日復盤**：一個 `(user, mode, trading_day)` 一則。
- [ ] **寫在交易績效頁**（008）：區間＝單日時，於逐筆表下方出現「今日復盤」區塊；
      區間跨日時不顯示（復盤以交易日為單位）。
- [ ] **讀在交易日記頁**：按時間讀回顧，與逐筆交易並存。
- [ ] **系統自動帶入客觀事實**（唯讀，使用者不需重打）：當日損益、交易筆數／日配額、
      勝率、最長連續虧損筆數、當日啟動緊急停止次數。
- [ ] 使用者只填**主觀**的部分，且為**結構化提示而非空白框**（空白框永遠不會被填）：
      1. 執行紀律：今天有照計畫走嗎？　2. 情緒狀態　3. 明天要調整的一件事。
- [ ] 未填寫時在頁面上有輕量提示（例如「尚未填寫」徽章），但**不得**用強制或阻擋的方式催填。
- [ ] 已填寫者可再編輯；編輯不改動當初帶入的客觀數據（客觀值以當時快照為準，
      避免事後補單導致歷史復盤的數字被改寫）。
- [ ] mode 分離：sim 與 real 各自的交易日各自一則。

### 不要動到的部分

- `Trade` 的 `note`／`tags` 維持現狀——每筆層級不變，本項只新增日層級。
- 交易日記頁既有的逐筆列表與手動補記表單不動。
- 不要把每日復盤做成「必填才能繼續交易」之類的阻擋機制（那是 F5 摩擦機制的範疇，
  且使用者未提出此需求）。

### 資料模型建議

新表 `DailyReview`：`(user_id, mode, trading_day)` 唯一鍵＋三個主觀欄位＋
客觀數據快照（JSON 或分欄皆可）＋ `created_at`／`updated_at`。
`trading_day` 沿用既有定義（含夜盤跨日），與 008 的區間篩選同源。

### 本機參考實作

`screenshots-target/07-交易績效-今日-目標.png` 下半部即為此區塊：
左側「系統帶入」一列客觀數據，下方三個結構化欄位，右上角「尚未填寫」徽章。

### 給工程師的實作提示

- 客觀數據直接重用 008 的區間彙總結果（同一次查詢即可，不必另外算）。
- 「最長連續虧損筆數」stats 目前沒有，但由當日已平倉序列即可算出，屬純函式範疇。
- 之後 F3（損益型提醒）觸發時，可以把當下的提醒事由也寫進當日復盤的客觀區——
  屆時「系統知道的」與「使用者感受到的」會在同一則裡對照，這是很有價值的組合，
  但**本項先不做**，避免與 F3 綁定排期。
